// Deal Review (the hero) — recreated from the Claude Design handoff
// (ProcureAI.html · artboard 03). Faithful static recreation.

import { DealReview } from "@/components/procureai/DealReview";

export default function ResultsPage() {
  return <DealReview />;
}
