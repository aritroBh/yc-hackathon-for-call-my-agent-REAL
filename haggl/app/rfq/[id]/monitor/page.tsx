// Live Call Monitor — recreated from the Claude Design handoff
// (ProcureAI.html · artboard 02). Faithful static recreation.

import { LiveCallMonitor } from "@/components/procureai/LiveCallMonitor";

export default function MonitorPage() {
  return <LiveCallMonitor />;
}
